export type Button = {
    button: string,
    value: string
}

export type Game = {
    id: number,
    title: string,
    publisher: string,
    developer: string,
    category: string,
    release_date: string,
    cover_url: string,
    players: number,
    description: string,
    rom: boolean
}